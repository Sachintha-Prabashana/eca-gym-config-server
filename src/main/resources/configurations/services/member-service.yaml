spring:
  mvc:
    problemdetails:
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:12500/db-fitbuddy-member
    username: postgres
    password: Sachintha@2003
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 8001
  forward-headers-strategy: framework

file:
  upload-dir: ${user.home}/.ijse/eca/fitbuddy/profile-images

jwt:
  secret: a-very-secure-jwt-secret-key-that-should-be-at-least-256-bits-long
  expiration: 900000 # 15 minutes in ms
  refresh-expiration: 604800000 # 7 days in ms